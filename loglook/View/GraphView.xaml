<ResourceDictionary x:Class="View.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
             xmlns:lc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <DataTemplate DataType="{x:Type vm:GraphViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="400"></RowDefinition>
            </Grid.RowDefinitions>
            <lc:CartesianChart Grid.Row="1" LegendLocation="Bottom" DataClickCommand="{Binding InspectDataCommand}" Series="{Binding SeriesCollection}" >
                <lc:CartesianChart.AxisX>
                    <lc:Axis LabelFormatter="{Binding Formatter}"></lc:Axis>
                </lc:CartesianChart.AxisX>
                <lc:CartesianChart.AxisY>
                    <!--setting the axis unit improved the labels rounding rule-->
                    <lc:Axis Unit="1"></lc:Axis>
                </lc:CartesianChart.AxisY>
            </lc:CartesianChart>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
